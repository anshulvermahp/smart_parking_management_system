<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= parking.name %> - Details
    </title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Google+Sans:wght@400;500;700&display=swap" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <style>
        :root {
            --sidebar-width: 400px;
            --navbar-height: 70px;
        }

        body {
            font-family: 'Google Sans', sans-serif;
            background: #f0f2f5;
            height: 100vh;
            overflow: hidden;
            display: flex;
            flex-direction: column;
        }

        /* Navbar Tweaks */
        .navbar {
            height: var(--navbar-height);
            background: white;
            z-index: 1000;
            box-shadow: 0 1px 2px rgba(0, 0, 0, 0.05);
        }

        /* Main Grid Wrapper */
        .dashboard-container {
            flex: 1;
            padding: 1rem;
            display: grid;
            grid-template-columns: var(--sidebar-width) 1fr;
            gap: 1rem;
            height: calc(100vh - var(--navbar-height));
            overflow: hidden;
        }

        /* Panels */
        .panel {
            background: white;
            border-radius: 16px;
            box-shadow: 0 2px 12px rgba(0, 0, 0, 0.03);
            border: 1px solid rgba(0, 0, 0, 0.04);
            display: flex;
            flex-direction: column;
            overflow: hidden;
        }

        .left-panel {
            display: flex;
            flex-direction: column;
            gap: 1rem;
            background: transparent;
            box-shadow: none;
            border: none;
            overflow-y: auto;
            /* Allow scroll only in left panel if content is too tall */
            padding-right: 4px;
            /* Space for scrollbar */
        }

        .left-panel::-webkit-scrollbar {
            width: 4px;
        }

        .left-panel::-webkit-scrollbar-thumb {
            background: #ccc;
            border-radius: 4px;
        }

        /* Hero Card */
        .hero-card {
            background: white;
            border-radius: 16px;
            overflow: hidden;
            flex-shrink: 0;
        }

        .hero-img {
            width: 100%;
            height: 180px;
            object-fit: cover;
        }

        .info-content {
            padding: 1.25rem;
        }

        /* Stats Grid */
        .stats-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 0.75rem;
            flex-shrink: 0;
        }

        .stat-card {
            background: white;
            padding: 1rem;
            border-radius: 12px;
            border: 1px solid rgba(0, 0, 0, 0.05);
            text-align: center;
        }

        .stat-label {
            font-size: 0.75rem;
            text-transform: uppercase;
            color: #6c757d;
            font-weight: 700;
            letter-spacing: 0.5px;
            margin-bottom: 0.25rem;
        }

        .stat-value {
            font-size: 1.5rem;
            font-weight: 700;
            color: #212529;
            margin: 0;
        }

        /* Map & Details Card */
        .details-card {
            background: white;
            border-radius: 16px;
            padding: 1.25rem;
            flex-shrink: 0;
        }

        .map-iframe-container {
            position: relative;
            width: 100%;
            height: 150px;
            border-radius: 8px;
            overflow: hidden;
            margin-top: 1rem;
        }

        .map-iframe-container iframe {
            width: 100%;
            height: 100%;
            border: 0;
        }

        /* Right Panel (Bookings) */
        .bookings-panel {
            height: 100%;
        }

        .panel-header {
            padding: 1.25rem 1.5rem;
            border-bottom: 1px solid #eee;
            background: white;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .table-wrapper {
            flex: 1;
            overflow-y: auto;
            position: relative;
        }

        .table thead th {
            position: sticky;
            top: 0;
            background: #f8f9fa;
            z-index: 1;
            font-size: 0.8rem;
            text-transform: uppercase;
            font-weight: 700;
            letter-spacing: 0.5px;
            padding-top: 1rem;
            padding-bottom: 1rem;
        }

        /* Custom Badge */
        .badge-status {
            padding: 6px 10px;
            border-radius: 6px;
            font-weight: 600;
            font-size: 0.8rem;
        }

        /* Amenities Tags */
        .amenity-tag {
            font-size: 0.8rem;
            padding: 4px 10px;
            background: #f8f9fa;
            border: 1px solid #e9ecef;
            border-radius: 20px;
            color: #495057;
        }

        /* Mobile Responsiveness */
        @media (max-width: 992px) {
            body {
                height: auto;
                overflow: auto;
            }

            .dashboard-container {
                display: flex;
                flex-direction: column;
                height: auto;
                overflow: visible;
                gap: 1.5rem;
            }

            .left-panel {
                overflow: visible;
                padding-right: 0;
            }

            .stats-grid {
                grid-template-columns: 1fr 1fr;
            }

            .bookings-panel {
                height: auto;
                overflow: visible;
            }

            .table-wrapper {
                max-height: 500px;
                /* Limit height on mobile so it doesn't scroll forever */
            }

            /* Navbar adjustments */
            .navbar {
                height: auto;
                padding-top: 1rem;
                padding-bottom: 1rem;
            }
        }

        @media (max-width: 576px) {
            .navbar-brand span {
                display: inline-block;
                /* Ensure title shows */
            }

            .table-wrapper {
                max-height: 400px;
            }

            .stats-grid {
                grid-template-columns: 1fr;
                /* Stack stats on very small screens */
            }
        }
    </style>
</head>

<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg border-bottom px-4">
        <a class="navbar-brand fw-bold d-flex align-items-center gap-2" href="/">
            <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24">
                <path fill="#222" d="M12 2a10 10 0 100 20 10 10 0 000-20z" />
            </svg>
            <span>Parking.com</span>
        </a>
        <div class="ms-auto">
            <!-- Desktop Menu -->
            <div class="d-none d-lg-flex align-items-center gap-3">
                <a href="/dashboard/<%= parking._id %>/edit" class="btn btn-sm btn-outline-primary fw-medium">
                    <i class="bi bi-pencil-square me-1"></i> Edit Parking
                </a>
                <form action="/dashboard/<%= parking._id %>/delete" method="POST"
                    onsubmit="return confirm('Are you sure you want to delete this parking listing?');" class="m-0">
                    <button type="submit" class="btn btn-sm btn-outline-danger fw-medium">
                        <i class="bi bi-trash me-1"></i> Delete Parking
                    </button>
                </form>
                <div class="vr mx-1"></div>
                <a href="/dashboard" class="btn btn-sm btn-dark fw-medium">Back to Dashboard</a>
            </div>

            <!-- Mobile Menu -->
            <div class="d-lg-none dropdown">
                <button class="btn btn-light rounded-circle shadow-sm" type="button" data-bs-toggle="dropdown"
                    aria-expanded="false"
                    style="width: 40px; height: 40px; display: flex; align-items: center; justify-content: center;">
                    <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none"
                        stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                        <circle cx="12" cy="12" r="1"></circle>
                        <circle cx="12" cy="5" r="1"></circle>
                        <circle cx="12" cy="19" r="1"></circle>
                    </svg>
                </button>
                <ul class="dropdown-menu dropdown-menu-end shadow-lg border-0 mt-2 p-2"
                    style="border-radius: 12px; min-width: 200px;">
                    <li>
                        <a class="dropdown-item rounded-2 py-2 mb-1" href="/dashboard/<%= parking._id %>/edit">
                            <i class="bi bi-pencil-square me-2 text-primary"></i> Edit Parking
                        </a>
                    </li>
                    <li>
                        <form action="/dashboard/<%= parking._id %>/delete" method="POST"
                            onsubmit="return confirm('Are you sure you want to delete this parking listing?');"
                            class="m-0">
                            <button type="submit" class="dropdown-item rounded-2 py-2 mb-1 text-danger">
                                <i class="bi bi-trash me-2"></i> Delete Parking
                            </button>
                        </form>
                    </li>
                    <li>
                        <hr class="dropdown-divider opacity-10 my-1">
                    </li>
                    <li>
                        <a class="dropdown-item rounded-2 py-2" href="/dashboard">
                            <i class="bi bi-arrow-left me-2"></i> Back to Dashboard
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Main Grid Layout -->
    <div class="dashboard-container">

        <!-- Left Column: Details -->
        <div class="left-panel">

            <!-- Hero Wrapper -->
            <div class="hero-card shadow-sm">
                <% if (parking.images && parking.images.length> 0) { %>
                    <img src="<%= parking.images[0] %>" class="hero-img" alt="<%= parking.name %>">
                    <% } else { %>
                        <div
                            style="height: 180px; background: #e9ecef; display: flex; align-items: center; justify-content: center;">
                            <span class="text-muted">No Image Available</span>
                        </div>
                        <% } %>
                            <div class="info-content">
                                <div class="d-flex justify-content-between align-items-start mb-2">
                                    <h4 class="fw-bold mb-0 text-truncate">
                                        <%= parking.name %>
                                    </h4>
                                    <span
                                        class="badge bg-success bg-opacity-10 text-success border border-success px-2 py-1 rounded-3">Active</span>
                                </div>
                                <p class="text-muted small mb-0">
                                    <i class="bi bi-geo-alt-fill text-danger me-1"></i>
                                    <%= parking.address.street %>, <%= parking.address.city %>
                                </p>
                            </div>
            </div>

            <!-- Stats Grid -->
            <div class="stats-grid">
                <div class="stat-card shadow-sm">
                    <div class="stat-label">Total Slots</div>
                    <div class="stat-value">
                        <%= parking.totalSlots %>
                    </div>
                </div>
                <div class="stat-card shadow-sm">
                    <div class="stat-label">Available</div>
                    <div class="stat-value text-success">
                        <%= parking.availableSlots %>
                    </div>
                </div>
                <div class="stat-card shadow-sm">
                    <div class="stat-label">Rate / Hr</div>
                    <div class="stat-value">₹<%= parking.pricing.hourly %>
                    </div>
                </div>
                <div class="stat-card shadow-sm">
                    <div class="stat-label">Revenue</div>
                    <div class="stat-value text-primary">₹<%= revenue || 0 %>
                    </div>
                </div>
            </div>

            <!-- More Details -->
            <div class="details-card shadow-sm">
                <div class="mb-3">
                    <h6 class="text-uppercase small fw-bold text-muted mb-2">Operating Hours</h6>
                    <div class="d-flex justify-content-between text-dark small fw-medium">
                        <span><i class="bi bi-clock me-2 text-primary"></i>Opens: <%= parking.operationalHours.open %>
                        </span>
                        <span>Closes: <%= parking.operationalHours.close %></span>
                    </div>
                </div>

                <hr class="my-3 text-muted opacity-25">

                <div class="mb-3">
                    <h6 class="text-uppercase small fw-bold text-muted mb-2">Amenities</h6>
                    <div class="d-flex flex-wrap gap-2">
                        <% if (parking.amenities && parking.amenities.length> 0) { %>
                            <% parking.amenities.forEach(function(item) { %>
                                <div class="amenity-tag">
                                    <%= item %>
                                </div>
                                <% }); %>
                                    <% } else { %>
                                        <span class="text-muted small">No amenities listed</span>
                                        <% } %>
                    </div>
                </div>

                <% if (mapQuery && mapQuery.replace(/,/g, '' ).trim().length> 0) { %>
                    <div class="map-iframe-container">
                        <iframe src="https://maps.google.com/maps?q=<%= mapQuery %>&hl=en&z=14&output=embed"
                            allowfullscreen></iframe>
                    </div>
                    <a href="<%= parking.mapLink || '#' %>" target="_blank"
                        class="btn btn-light btn-sm w-100 mt-2 text-primary fw-medium border">
                        <i class="bi bi-box-arrow-up-right me-1"></i> Open in Google Maps
                    </a>
                    <% } %>
            </div>

        </div>

        <!-- Right Column: Bookings Table -->
        <div class="panel bookings-panel">
            <div class="panel-header">
                <h5 class="fw-bold mb-0">Booking Requests & History</h5>
                <div class="badge bg-primary bg-opacity-10 text-primary px-3 py-2 rounded-pill">
                    Total Bookings: <%= bookings ? bookings.length : 0 %>
                </div>
            </div>

            <div class="table-wrapper">
                <table class="table table-hover align-middle mb-0">
                    <thead>
                        <tr>
                            <th class="ps-4">User</th>
                            <th>Date & Time</th>
                            <th>Slots</th>
                            <th>Amount</th>
                            <th>Status</th>
                            <th class="text-end pe-4">Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% if (bookings && bookings.length> 0) { %>
                            <% bookings.forEach(booking=> { %>
                                <tr>
                                    <td class="ps-4">
                                        <div class="fw-bold text-dark">
                                            <%= booking.user ? booking.user.username : 'Unknown User' %>
                                        </div>
                                        <div class="small text-muted">
                                            <%= booking.user ? booking.user.phoneNumber : '' %>
                                        </div>
                                    </td>
                                    <td>
                                        <div class="fw-medium">
                                            <%= booking.date %>
                                        </div>
                                        <div class="small text-muted">
                                            <%= booking.time %>
                                        </div>
                                    </td>
                                    <td>
                                        <span
                                            class="badge bg-secondary bg-opacity-10 text-secondary border border-secondary border-opacity-25 rounded-pill px-3">
                                            <%= booking.slots %>
                                        </span>
                                    </td>
                                    <td class="fw-bold text-dark">₹<%= booking.amount %>
                                    </td>
                                    <td>
                                        <% if (booking.status==='confirmed' ) { %>
                                            <span
                                                class="badge-status bg-success bg-opacity-10 text-success border border-success border-opacity-25">Confirmed</span>
                                            <% } else if (booking.status==='cancelled' || booking.status==='rejected' )
                                                { %>
                                                <span
                                                    class="badge-status bg-danger bg-opacity-10 text-danger border border-danger border-opacity-25">Cancelled</span>
                                                <% } else { %>
                                                    <span
                                                        class="badge-status bg-warning bg-opacity-10 text-warning border border-warning border-opacity-25">Pending</span>
                                                    <% } %>
                                    </td>
                                    <td class="text-end pe-4">
                                        <% if (booking.status==='pending' ) { %>
                                            <div class="d-flex gap-2 justify-content-end">
                                                <form
                                                    action="/dashboard/<%= parking._id %>/booking/<%= booking._id %>/status"
                                                    method="POST" class="m-0">
                                                    <input type="hidden" name="status" value="confirmed">
                                                    <button type="submit"
                                                        class="btn btn-sm btn-success rounded-3 px-3">Accept</button>
                                                </form>
                                                <form
                                                    action="/dashboard/<%= parking._id %>/booking/<%= booking._id %>/status"
                                                    method="POST" class="m-0">
                                                    <input type="hidden" name="status" value="cancelled">
                                                    <button type="submit"
                                                        class="btn btn-sm btn-outline-danger rounded-3 px-3">Reject</button>
                                                </form>
                                            </div>
                                            <% } else { %>
                                                <span class="text-muted small">Completed</span>
                                                <% } %>
                                    </td>
                                </tr>
                                <% }) %>
                                    <% } else { %>
                                        <tr>
                                            <td colspan="6" class="text-center py-5 text-muted">
                                                <div class="d-flex flex-column align-items-center">
                                                    <i class="bi bi-inbox fs-1 mb-2 opacity-50"></i>
                                                    <p class="mb-0">No booking requests found.</p>
                                                </div>
                                            </td>
                                        </tr>
                                        <% } %>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
    <!-- Bootstrap JS Bundle (Required for Dropdowns) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
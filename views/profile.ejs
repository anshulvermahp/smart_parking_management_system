<!DOCTYPE html>
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>
        <%= user.username %> • Profile
    </title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Google Sans', Arial, sans-serif;
            background: #fff;
            color: #111;
        }

        .card-modern {
            border-radius: 14px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.06);
        }

        .profile-photo {
            width: 140px;
            height: 140px;
            border-radius: 16px;
            object-fit: cover;
            border: 6px solid #fff;
            box-shadow: 0 8px 28px rgba(0, 0, 0, 0.06);
        }

        .sticker {
            background: #000;
            color: #fff;
            padding: 6px 12px;
            border-radius: 999px;
            font-weight: 700;
        }

        .muted {
            color: #6b6b6b;
        }

        .btn-primary-dark {
            background: #000;
            color: #fff;
            border: none;
        }
    </style>
</head>

<body>
    <nav class="navbar navbar-expand-lg py-3">
        <div class="container">
            <a class="navbar-brand d-flex align-items-center" href="/">
                <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24">
                    <path fill="#222" d="M12 2a10 10 0 100 20 10 10 0 000-20z" />
                </svg>
                <span style="margin-left:10px;color:#222;font-weight:700;">Parking.com</span>
            </a>
            <div class="ms-auto">
                <a class="btn btn-outline-dark" href="/map">Find parking</a>
            </div>
        </div>
    </nav>

    <main class="container my-5">
        <div class="row g-4">
            <div class="col-lg-4">
                <div class="card card-modern p-4 align-items-center text-center">
                    <% if (user.profilePicture) { %>
                        <img src="<%= user.profilePicture %>" alt="Profile" class="profile-photo mb-3" />
                        <% } else { %>
                            <div class="profile-photo mb-3 " style="background:#f1f1f1; display:inline-block;"></div>
                            <% } %>
                                <h4 style="margin-bottom:2px;">
                                    <%= user.username %>
                                </h4>
                                <p class="muted small mb-3">
                                    <%= user.email %>
                                </p>
                                <div class="d-flex gap-2 align-items-center">
                                    <a href="/profile/edit-profile" class="btn btn-primary-dark flex-grow-1">Edit profile</a>
                                    <% if (user.owner) { %>
                                        <a href="/admin/add-parking" class="btn btn-outline-dark">Add slot</a>
                                        <% } else { %>
                                            <a href="/owners/owner_registration" class="btn btn-outline-dark">Become
                                                owner</a>
                                            <% } %>
                                </div>
                </div>

                <div class="card card-modern p-4 mt-4 flex align-items-center">
                    <h6 style="margin-bottom:12px; ">Account</h6>
                    <p class="mb-1"><strong>Phone</strong></p>
                    <p class="muted small mb-2">
                        <%= user.phoneNumber || '—' %>
                    </p>
                    <p class="mb-1"><strong>Joined</strong></p>
                    <p class="muted small">
                        <%= user.createdAt ? new Date(user.createdAt).toLocaleDateString() : '—' %>
                    </p>
                </div>
            </div>

            <div class="col-lg-8">
                <div class="card card-modern p-4">
                    <div class="d-flex justify-content-between align-items-center mb-3">
                        <h5 style="margin:0;">Recent bookings</h5>
                        <div class="sticker">Recent</div>
                    </div>
                    <% if (user.bookings && user.bookings.length) { %>
                        <ul class="list-group list-group-flush">
                            <% user.bookings.forEach(function(b){ %>
                                <li class="list-group-item d-flex justify-content-between align-items-center">
                                    <div>
                                        <div style="font-weight:700;">
                                            <%= b.parkingName || 'Booking' %>
                                        </div>
                                        <div class="muted small">
                                            <%= b.date || b.slot || '' %>
                                        </div>
                                    </div>
                                    <div class="muted small">
                                        <%= b.status || 'Confirmed' %>
                                    </div>
                                </li>
                                <% }); %>
                        </ul>
                        <% } else { %>
                            <div class="text-center py-5 muted">You haven't booked any slots yet.</div>
                            <% } %>
                </div>
            </div>
        </div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
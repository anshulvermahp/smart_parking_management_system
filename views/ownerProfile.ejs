<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>
        <%= owner.username %> • Owner Profile
    </title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Google Sans', Arial, sans-serif;
            background: #fff;
            color: #111;
        }

        .card-modern {
            border-radius: 14px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.06);
        }

        .profile-photo {
            width: 140px;
            height: 140px;
            border-radius: 16px;
            object-fit: cover;
            border: 6px solid #fff;
            box-shadow: 0 8px 28px rgba(0, 0, 0, 0.06);
        }

        .muted {
            color: #6b6b6b;
        }

        .btn-primary-dark {
            background: #000;
            color: #fff;
            border: none;
        }

        .verification-banner { border-radius:10px; }
    </style>
</head>

<body>
    <nav class="navbar navbar-expand-lg py-3">
        <div class="container">
            <a class="navbar-brand d-flex align-items-center" href="/">
                <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24">
                    <path fill="#222" d="M12 2a10 10 0 100 20 10 10 0 000-20z" />
                </svg>
                <span style="margin-left:10px;color:#222;font-weight:700;">Parking.com</span>
            </a>
            <div class="ms-auto">
                <a class="btn btn-outline-dark" href="/map">Find parking</a>
            </div>
        </div>
    </nav>

    <main class="container my-5">
        <% if (typeof success !== 'undefined') { %>
            <div class="alert alert-success"><%= success %></div>
        <% } %>
        <% if (typeof error !== 'undefined') { %>
            <div class="alert alert-danger"><%= error %></div>
        <% } %>

        <% if (owner && (owner.verificationStatus === 'pending' || owner.isVerified === false)) { %>
            <div class="alert alert-info verification-banner">
                <h6 class="mb-1">Profile under review</h6>
                <p class="mb-0 small">Your profile is currently under review. Our team will verify the originality of your profile documents. You will be notified once the verification is complete.</p>
            </div>
        <% } else if (owner && owner.verificationStatus === 'rejected') { %>
            <div class="alert alert-warning verification-banner">
                <h6 class="mb-1">Verification rejected</h6>
                <p class="mb-2 small">Your verification was not approved. Please review your submitted documents and resubmit.</p>
                <a href="/owners/edit-profile" class="btn btn-sm btn-outline-dark">Resubmit documents / Edit profile</a>
            </div>
        <% } else if (owner && owner.isVerified) { %>
            <div class="d-flex align-items-center mb-3">
                <span class="badge bg-success me-2">Verified</span>
                <small class="text-muted">This account has been verified by our team.</small>
            </div>
        <% } %>

        <div class="row g-4">
            <div class="col-lg-4">
                <div class="card card-modern p-4 align-items-center text-center">
                    <% if (owner.profileUrl) { %>
                        <img src="<%= owner.profileUrl %>" alt="Profile" class="profile-photo mb-3" />
                    <% } else { %>
                        <div class="profile-photo mb-3 " style="background:#f1f1f1; display:inline-block;"></div>
                    <% } %>
                    <h4 style="margin-bottom:2px;"><%= owner.username %></h4>
                    <p class="muted small mb-3"><%= owner.email %></p>
                    <div class="d-flex gap-2 align-items-center w-100">
                        <a href="/owners/edit-profile" class="btn btn-primary-dark flex-grow-1">Edit profile</a>
                    </div>
                </div>

                <div class="card card-modern p-4 mt-4 flex align-items-center">
                    <h6 style="margin-bottom:12px; ">Account</h6>
                    <p class="mb-1"><strong>Phone</strong></p>
                    <p class="muted small mb-2"><%= owner.contactNumber || '—' %></p>
                    <p class="mb-1"><strong>Registered</strong></p>
                    <p class="muted small"><%= owner.createdAt ? new Date(owner.createdAt).toLocaleDateString() : '—' %></p>
                </div>
            </div>

            <div class="col-lg-8">
                <div class="card card-modern p-4">
                    <h5 style="margin:0 0 12px 0;">Owner details</h5>
                    <div class="row">
                        <div class="col-md-6 mb-2"><strong>Username:</strong>
                            <div class="muted"><%= owner.username %></div>
                        </div>
                        <div class="col-md-6 mb-2"><strong>Email:</strong>
                            <div class="muted"><%= owner.email %></div>
                        </div>
                        <div class="col-md-6 mb-2"><strong>Contact Number:</strong>
                            <div class="muted"><%= owner.contactNumber || '—' %></div>
                        </div>
                        <div class="col-md-12 mb-2"><strong>Proof of Ownership:</strong>
                            <div class="muted small mt-1">
                                <% if (owner.proofUrl) { %>
                                    <a href="<%= owner.proofUrl %>" target="_blank">View proof</a>
                                <% } else { %>
                                    <%= owner.proofOfOwnership || '—' %>
                                <% } %>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
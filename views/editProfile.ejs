<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Edit Profile</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { font-family: 'Google Sans', Arial, sans-serif; background:#fff; color:#111; }
        .navbar { background: transparent !important; }
        .card-modern { border-radius:14px; box-shadow:0 10px 30px rgba(0,0,0,0.06); }
        .label { font-weight:700; color:#222; }
        .sticker { background:#000; color:#fff; padding:6px 10px; border-radius:999px; font-weight:700; font-size:0.9rem; }
        .profile-photo { width:120px; height:120px; border-radius:16px; object-fit:cover; border:4px solid #fff; box-shadow:0 6px 20px rgba(0,0,0,0.08); }
        .btn-primary-dark { background:#000; color:#fff; border:none; }
        .btn-outline-dark { border:1px solid #e8e8e8; background:#fff; color:#111; }
        .form-control { border-radius:8px; }
    </style>
</head>
<body>
    <!-- NAVBAR (simple, consistent) -->
    <nav class="navbar navbar-expand-lg py-3">
        <div class="container">
            <a class="navbar-brand d-flex align-items-center" href="/">
                <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24"><path fill="#222" d="M12 2a10 10 0 100 20 10 10 0 000-20z"/></svg>
                <span style="margin-left:10px;color:#222;font-weight:700;">Parking.com</span>
            </a>
            <div class="ms-auto">
                <a class="btn btn-outline-dark" href="/profile">Back to profile</a>
            </div>
        </div>
    </nav>

    <main class="container my-5">
        <div class="row">
            <div class="col-lg-8">
                <div class="card card-modern p-4">
                    <div class="d-flex justify-content-between align-items-start mb-3">
                        <h3 style="margin:0;">Edit Profile</h3>
                   
                    </div>
                    <form action="/profile/edit-profile" method="POST" enctype="multipart/form-data">
                        <div class="row g-3">
                            <div class="col-md-6">
                                <label class="label">Username</label>
                                <input class="form-control" type="text" name="username" value="<%= user.username %>" required />
                            </div>
                            <div class="col-md-6">
                                <label class="label">Email</label>
                                <input class="form-control" type="email" name="email" value="<%= user.email %>" required />
                            </div>
                            <div class="col-md-6">
                                <label class="label">Phone</label>
                                <input class="form-control" type="text" name="phoneNumber" value="<%= user.phoneNumber %>" />
                            </div>
                            <div class="col-md-6">
                                <label class="label">Profile Picture</label>
                                <input class="form-control" type="file" name="profilePicture" accept="image/*" />
                            </div>
                            <div class="col-md-6">
                                <label class="label">Current Password</label>
                                <input class="form-control" type="password" name="oldPassword" placeholder="Leave blank to keep" />
                            </div>
                            <div class="col-md-6">
                                <label class="label">New Password</label>
                                <input class="form-control" type="password" name="password" />
                            </div>
                        </div>
                        <div class="mt-4 d-flex gap-2 justify-content-center">
                            <button class="btn btn-primary-dark px-4" type="submit">Save changes</button>
                            <a href="/profile" class="btn btn-outline-dark px-4">Cancel</a>
                        </div>
                    </form>
                </div>
            </div>

            <aside class="col-lg-4">
                <div class="card card-modern p-4 text-center align-items-center">
                    <% if (user.profilePicture) { %>
                        <img src="<%= user.profilePicture %>" alt="Profile" class="profile-photo mb-3" />
                    <% } else { %>
                        <div class="profile-photo mb-3" style="background:#f1f1f1; display:inline-block;"></div>
                    <% } %>
                    <h5 style="margin-bottom:0;"><%= user.username %></h5>
                    <p class="text-muted small mb-2"><%= user.email %></p>
                    <div class="mt-2">
                        <a href="/new-parking" class="btn btn-outline-dark w-100 mb-2">Add parking slot</a>
                        <a href="/bookings" class="btn btn-outline-dark w-100">My bookings</a>
                    </div>
                </div>
            </aside>
        </div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>